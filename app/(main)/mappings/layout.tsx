import { Metadata } from "next";
export const metadata: Metadata = { title: "Zone Mappings" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
